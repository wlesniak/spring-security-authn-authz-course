This demos requires an SMTP server for email verification, you can add them to the application.yml file
Or if you want to disable email verification set disableEmailVerification: true
property in the application.yml file.